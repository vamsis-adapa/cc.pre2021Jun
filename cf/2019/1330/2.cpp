#include<bits/stdc++.h>
using namespace std;

#define ll long long int

ll t;
void func()
{
    ll n; 
    cin >>n;
    vector<int> arr(200005,0);
    int i =0;
    while( n--)
    {
        ll a;
        cin >> a;
        if (arr[a]==1)
        {
            break;
        }
    }

}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> t;
    
    while(t--)
    {
        
    }
    return 0;
}